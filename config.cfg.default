# default configuration to apply latest changes with db ./run.sh

# Script action
PATCH=1   # 0 = off, 1 = on
# INSTALL=0 # 0 = off, 1 = on

# Connection settings
DBUSER='postgres'
DBHOST='127.0.0.1'
DATABASE='demodb'
DBPORT=5432

# Other options
SILENT=1 # 0 = ask for confirmation, 1 = just do the stuff
